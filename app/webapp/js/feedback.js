document.querySelector('#feedbackForm').addEventListener('submit',async e=>{e.preventDefault();const form=e.target,b=form.querySelector('button');b.disabled=true;const data=new FormData(form);try{const r=await fetch(location.pathname,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rating:Number(data.get('rating')),comment:data.get('comment')})}),j=await r.json();if(!r.ok)throw new Error(j.error||'Не удалось отправить.');document.querySelector('#feedbackResult').textContent='Спасибо за оценку!';form.hidden=true;}catch(err){document.querySelector('#feedbackResult').textContent=err.message;b.disabled=false;}});
